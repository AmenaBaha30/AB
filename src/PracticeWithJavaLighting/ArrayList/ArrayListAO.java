package PracticeWithJavaLighting.ArrayList;

public class ArrayListAO {
    public static void main(String[] args) {
        // Method is a block of code which executes when it's called upon
        // to call a static method name of the method following () with ;

        sum();
        sum();

    }

    static void sum(){ // To create a static method Static word Return value type if there is
        // no value use the VOID return type and then Name of the method following () and then
        // {}
        //
        int x=20;
        int y=30;
        int Sum = x+y;
        System.out.println(Sum);
    }
}
