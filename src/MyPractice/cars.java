package MyPractice;

import java.util.ArrayList; // import the Array Class

public class cars {
    public static void main(String[] args) {
        ArrayList<String> cars = new ArrayList<String>(); // Create an ArrayList object
        cars.add(" Volvo ");
        cars.add(" BMW ");
        cars.add(" Ford ");
        cars.add(" Mazda ");
       // cars.clear();
        //cars.size();
        //System.out.println(cars.size());

        // To remove an element, use the remove() method and refer
        // to the index number:
        //cars.remove(3);

        //To remove all the elements in the ArrayList,
        // use the clear() method:
        //cars.clear();

        // To find out how many elements an ArrayList have,
        // use the size method

    // To modify an element, use the set() method
        // and refer to the index number:
        //cars.set(3, " Rav4 ");

        //cars.add(0, "Ford"); // Insert element at the
        // beginning of the list (0)


             //cars.get();
            //To access an element in the ArrayList,
            //use the get() method and refer to the index number:
            // System.out.println(cars.get(0));


    }
}